# Vercel installs with npm 10, which crashes ("Cannot read properties of null (reading 'edgesOut')")
# while resolving this tree without a lockfile (package-lock.json is gitignored). Legacy peer
# resolution sidesteps the crash; npm 11 resolves the same tree fine either way.
legacy-peer-deps=true
